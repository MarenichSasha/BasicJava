// массив на 100 элементов

import java.util.Random;

public class Main {

    public static void main(String[] args) {
        long start = System.currentTimeMillis();
        int min = 10;
        int max = 30;

        // массив на 100 элементов

        int a = 100;
        int[] arr;
        System.out.println("First array: ");
        Random r = new Random();
        arr = new int[a];
        for (int i = 0; i < a; i++) {
            arr[i] = min + r.nextInt(max - min);
            System.out.print(arr[i] + "; ");
        }
        for (int i = 0; i < arr.length; i++) {
            for (int j = 0; j < arr.length; j++) {
                if (arr[i] < arr[j]) {
                    int b = arr[i];
                    arr[i] = arr[j];
                    arr[j] = b;
                }
            }
        }
        long end =System.currentTimeMillis();
        System.out.println();
        System.out.printf("Sort [%d] duration %d = ms \n" , arr.length , (end - start));
        System.out.println("New array: ");
        for (int i = 0; i < a; i++) {
            System.out.print(arr[i] + "; ");
        }
    }
}

// массив 1000 элементов


import java.util.Random;

public class Main {

    public static void main(String[] args) {
        long start = System.currentTimeMillis();
        int min = 10;
        int max = 110;

        // массив на 1000 элементов

        int a = 1000;
        int[] arr;
        System.out.println("First array: ");
        Random r = new Random();
        arr = new int[a];
        for (int i = 0; i < a; i++) {
            arr[i] = min + r.nextInt(max - min);
            System.out.print(arr[i] + "; ");
        }
        for (int i = 0; i < arr.length; i++) {
            for (int j = 0; j < arr.length; j++) {
                if (arr[i] < arr[j]) {
                    int b = arr[i];
                    arr[i] = arr[j];
                    arr[j] = b;
                }
            }
        }
        long end =System.currentTimeMillis();
        System.out.println();
        System.out.printf("Sort [%d] duration %d = ms \n" , arr.length , (end - start));
        System.out.println("New array: ");
        for (int i = 0; i < a; i++) {
            System.out.print(arr[i] + "; ");
        }
    }
}

// массив на 10 000 элементов


import java.util.Random;

public class Main {

    public static void main(String[] args) {
        long start = System.currentTimeMillis();
        int min = 10;
        int max = 110;

        // массив на 10 000 элементов

        int a = 10000;
        int[] arr;
        System.out.println("First array: ");
        Random r = new Random();
        arr = new int[a];
        for (int i = 0; i < a; i++) {
            arr[i] = min + r.nextInt(max - min);
            System.out.print(arr[i] + "; ");
        }
        for (int i = 0; i < arr.length; i++) {
            for (int j = 0; j < arr.length; j++) {
                if (arr[i] < arr[j]) {
                    int b = arr[i];
                    arr[i] = arr[j];
                    arr[j] = b;
                }
            }
        }
        long end =System.currentTimeMillis();
        System.out.println();
        System.out.printf("Sort [%d] duration %d = ms \n" , arr.length , (end - start));
        System.out.println("New array: ");
        for (int i = 0; i < a; i++) {
            System.out.print(arr[i] + "; ");
        }
    }
}

// массив на 100 000 элементов


import java.util.Random;

public class Main {

    public static void main(String[] args) {
        long start = System.currentTimeMillis();
        int min = 10;
        int max = 110;

        // массив на 100 000 элементов

        int a = 100000;
        int[] arr;
        System.out.println("First array: ");
        Random r = new Random();
        arr = new int[a];
        for (int i = 0; i < a; i++) {
            arr[i] = min + r.nextInt(max - min);
            System.out.print(arr[i] + "; ");
        }
        for (int i = 0; i < arr.length; i++) {
            for (int j = 0; j < arr.length; j++) {
                if (arr[i] < arr[j]) {
                    int b = arr[i];
                    arr[i] = arr[j];
                    arr[j] = b;
                }
            }
        }
        long end =System.currentTimeMillis();
        System.out.println();
        System.out.printf("Sort [%d] duration %d = ms \n" , arr.length , (end - start));
        System.out.println("New array: ");
        for (int i = 0; i < a; i++) {
            System.out.print(arr[i] + "; ");
        }
    }
}
